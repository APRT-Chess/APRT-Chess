import { PieceColor } from "../validate";

export function validateBishopMove(
	fromX: number,
	fromY: number,
	toX: number,
	toY: number,
	boardState: string[][],
	pieceColor: PieceColor
	): boolean {
		return true;
	}
